;; this file content some configration to emhance my own Experience
(setq indo-enable-flex-matching t)
(setq ring-bell-function 'ignore)
(setq ido-everywhere t)
(ido-mode 1)

(provide 'init-emhance)
